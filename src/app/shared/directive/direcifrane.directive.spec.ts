import { DirecifraneDirective } from './direcifrane.directive';

describe('DirecifraneDirective', () => {
  it('should create an instance', () => {
    const directive = new DirecifraneDirective();
    expect(directive).toBeTruthy();
  });
});
